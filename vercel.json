{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm --filter=@autonomux/web build",
  "installCommand": "corepack enable && pnpm install --frozen-lockfile=false",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- apps/web packages docs README.md package.json pnpm-workspace.yaml turbo.json vercel.json && exit 0 || exit 1"
}
