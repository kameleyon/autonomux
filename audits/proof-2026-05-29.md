# Proof — Copy + claims review · 2026-05-29 · Phase 1.0-A Foundation

**Scope:** every user-facing string across `apps/admin/app/**` and `packages/ui/src/**`. Banned-word check (PRD §13.3), claim substantiation, smart-quote consistency.

---

## Findings

### F-Proof-01 · Polish · "platform-specific" comment in code (not user-facing, but kept on the radar)

`packages/cipher/src/redact.ts:84` — `// platform-specific` is a code comment, not surfaced. PRD banned-word list applies to user-facing copy; code comments are fine. No action.

### F-Proof-02 · Minor · Smart-quote/typography polish: ASCII hyphen used between two phrases that read as a dash

`apps/admin/app/page.tsx:88` — "Internal &middot; access restricted to operators." uses `&middot;` consistently (good). Other surfaces use em-dash `—` (e.g. `(authed)/page.tsx:19` "Tenant list, drill-down, usage..."). Mixed punctuation between bullet copy is fine; just confirm the convention: `&middot;` for inline separator, `—` for clause break.

No fix required — current usage is consistent within each pattern.

### F-Proof-03 · Polish · Apostrophe encoding is consistent

`not-found.tsx:20` uses `&rsquo;` ("doesn't"). All other apostrophe occurrences in admin app render as plain ASCII inside JS strings (which Next.js will pass-through). Consistency on entity-vs-raw apostrophes is good. No action.

### F-Proof-04 · Minor · Sign-in copy is honest about state ("Auth wires in Phase 1.0-B")

`apps/admin/app/sign-in/page.tsx:23` — `setStatus("Auth wires in Phase 1.0-B. No request was sent.")`. Honest, non-misleading, no false success message. ✓ matches PRD §13 voice rule "says what it knows, asks when it doesn't, never invents."

### F-Proof-05 · Minor · Section descriptions on the dashboard cards re-paste PRD §3.2 verbatim — fine but slightly machine-y

`apps/admin/app/(authed)/page.tsx:19,25,31,37,43,49,55,61,67,73,79` — every card description is the PRD bullet point literal. Reads slightly cold for a polymath operator surface. Better Herald copy:
- "Tenants" → "Who pays, what they cost, what they're running."
- "Costs" → "Where the LLM bill goes — by tenant, model, sub-agent."
- "Audit log" → "Append-only, signed, 7 years."

Fix optional — current copy is accurate and not misleading; tightening is a Phase 1.0-C polish pass.

### F-Proof-06 · Polish · Footer's "© {year} {brandName}" defaults

`packages/ui/src/Footer/Footer.tsx:46` — fallback copyright is `© ${year} ${brandName}` (default `autonomux`). Lowercase brand name. PRD displays "Autonomux" (capital A) on the chameleon logo and headers. Inconsistent capitalization between Footer default and admin layout's `Autonom<em>ux</em>` brand wordmark.

Fix: default `brandName` to "Autonomux" instead of "autonomux" in `Footer.tsx:40`, OR pass `brandName` from each consumer explicitly. Minor.

### F-Proof-07 · None · Banned-word scan = clean

Searched `apps/admin` + `packages/ui` for `platform | unlock | fastest | best-in-class | synergy | leverage | world-class | magical` (case-insensitive). Zero matches in user-facing surfaces. ✓

### F-Proof-08 · None · No unsubstantiated claims surfaced

No quantitative claims appear on user-facing admin scaffold copy ("99.9% uptime," "fastest," "millions of," etc.). The PRD §13.4 substantiation rule cannot fire because no claim is made.

---

## Summary

Copy is restrained, honest about scaffold status, and avoids every banned word. The biggest tightening opportunity is humanizing the dashboard card descriptions (Herald pass at 1.0-C) and aligning the Footer brand default capitalization. Nothing blocks a push.

**Proof score:** 96 / 100 (Pass).
