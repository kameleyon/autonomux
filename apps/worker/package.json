{
  "name": "@autonomux/worker",
  "version": "0.1.0",
  "private": true,
  "description": "Autonomux — background worker. BullMQ-driven queue runner for AlterEgo sub-agents (Mailroom, Scheduler, Scribe, Oracle, Treasurer, Briefing, Audit, Cron). Deployed to Railway.",
  "type": "module",
  "main": "src/index.ts",
  "engines": {
    "node": ">=20.18.0"
  },
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "echo '@autonomux/worker: runtime is tsx — no compile step'",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@autonomux/cipher": "*",
    "@autonomux/db": "*",
    "@autonomux/logger": "*",
    "@autonomux/telemetry": "*",
    "@supabase/supabase-js": "2.47.10",
    "bullmq": "5.34.10",
    "ioredis": "5.4.2",
    "pino": "9.5.0",
    "dotenv": "8.6.0",
    "tsx": "4.19.2"
  },
  "devDependencies": {
    "@types/node": "22.10.5",
    "typescript": "5.8.2"
  }
}
