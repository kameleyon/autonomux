# ── dependencies ──
node_modules/
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# ── build outputs ──
.next/
out/
build/
dist/
.turbo/

# ── tests ──
coverage/
.nyc_output/

# ── logs ──
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── environment / secrets — NEVER COMMIT ──
.env
.env.*
!.env.example

# ── OS / IDE ──
.DS_Store
.vscode/
.idea/
*.swp

# ── vercel ──
.vercel/

# ── typescript ──
*.tsbuildinfo

# ── legacy autonomux-backend (pre-platform pivot, kept on disk for reference) ──
# These files predate the autonomux platform plan in PRD.md. They live here
# locally only; the new monorepo (Phase 1.0) will replace them with
# apps/web/, apps/admin/, apps/worker/, packages/*. Until Phase 1.0
# kicks off, ignore them so the repo starts clean.
/src/
/frontend/
/workspaces/
/schema.sql
/package.json
/package-lock.json
